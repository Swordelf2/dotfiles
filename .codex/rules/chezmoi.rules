prefix_rule(
    pattern = ["chezmoi", "apply", "--exclude=scripts", "--error-on-conflict"],
    decision = "allow",
    justification = "Allow conflict-safe dotfile apply without scripts",
    match = ["chezmoi apply --exclude=scripts --error-on-conflict"],
)
