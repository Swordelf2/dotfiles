# fzf-dirs.yazi

Overrides Yazi's `z` binding to search only directories with `fd` and fzf.
When searching from an ancestor, `~/.local` and `~/.cache` are excluded without hiding directories with those names elsewhere.
